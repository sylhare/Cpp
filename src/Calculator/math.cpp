#include "math.h"
#include <cmath>

int addTwo(int number)
{
    int value(number + 2);

    return value;
}

int add(int a, int b)
{
    return a + b;
}

double logarithm(int a, int b)
{
    return pow(a, b);
}

double squareroot(int a)
{
    return sqrt(a);
}

